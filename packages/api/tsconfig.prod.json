{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src"
  },
  // __tests__ is included in tsconfig.json to resolve @types/jest in the IDE
  "exclude": ["src/**/__tests__", "test"]
}
